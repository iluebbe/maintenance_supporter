---
category: Elements
---

The library's own outlined text field (HA-look): label, helper text, validation pattern, number/date/time types, min/max/step. Used across every dialog form.

```jsx
<ms-textfield label="Cost" type="number" step="0.01" min="0" helper="EUR"></ms-textfield>
```
