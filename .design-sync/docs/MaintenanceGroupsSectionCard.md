---
category: Cards
---

Task-group card: named groups (e.g. "Weekly routine", "Seasonal") with member task chips and per-group progress; supports opening members and group CRUD for admins.

```jsx
import { dsDemoHass, dsProps } from "maintenance-supporter-frontend";

<maintenance-groups-section-card ref={dsProps({ hass: dsDemoHass() })} />
```
