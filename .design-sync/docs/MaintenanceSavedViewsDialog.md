---
category: Dialogs
---

Manage saved task-list views (named filter presets): list, apply, save the current filters as a view, delete.

```jsx
import { dsDemoHass, dsProps } from "maintenance-supporter-frontend";

<maintenance-saved-views-dialog ref={dsProps({ hass: dsDemoHass() })} />
```
