{
  "name": "maintenance-supporter-e2e",
  "private": true,
  "description": "End-to-end user-story tests: drive a real Home Assistant + the integration in a real browser.",
  "scripts": {
    "test": "playwright test",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "1.62.0",
    "ffmpeg-static": "^5.3.0",
    "playwright": "1.62.0"
  },
  "dependencies": {
    "pixelmatch": "^7.2.0",
    "pngjs": "^7.0.0"
  }
}
